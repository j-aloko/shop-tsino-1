import React from 'react';

import Typography from '../typography/Typography';

function LatestDeal({ title }) {
  return <Typography text={title} variant="body2" textAlign="left" />;
}

export default LatestDeal;
